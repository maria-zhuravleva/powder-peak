<%- include('../partials/html-head') %>
<link rel="stylesheet" href="../../public/stylesheets/resorts/index.css" />
<%- include('../partials/nav') %>

<h1>All Resorts🗻</h1>

<main>
  <h1>Resort List</h1>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Location</th>
        <!-- <th>Difficulty Level</th>
        <th>Amenities</th> -->
      </tr>
    </thead>
    <tbody>
      <% resorts.forEach(resort => { %>
        <tr>
          <td> <a href="/resorts/<%= resort._id %>">
            <%= resort.name %>
          </a></td>
          <td><%= resort.location %></td>
          <!-- <td><%# resort.difficultyLevel %></td>
          <td><%# resort.amenities.join(', ') %></td>   -->
          <td><a href="/resorts/<%= resort._id %>">DETAILS</a></td>
        </tr>
        <% }) %>
    </tbody>
  </table>
  <div>
    <a 
    href="/resorts/new">
    <button type="submit">Add New Resort</button>
    </a>
  </div>
</main>

<%- include('../partials/footer') %>