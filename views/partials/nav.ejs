  <% if (!user) { %>
    <script defer src="https://accounts.google.com/gsi/client"></script>
  <% } %>
</head>
<body>
  <nav>
    <div class="top-bar">
      <div class="logo">pd.</div>
      <div class="menu">
        <ul>
          <li>
            <a href="/" class=<%= title === 'Home Page' ? 'active' : '' %>>home</a>
          </li>
          <li>
            <a href="/resorts" class="<%= title === 'Powder Peak' ? 'active' : '' %>">resorts</a>
          </li>
          <li>
            <% if (user && user.profile) { %>
              <a href="/profiles/<%= user.profile._id %>" class=<%= title === 'Profile' ? 'active' : '' %>>profile</a>
            <% } %>            
          </li>
          <li>
            <% if (user) { %>
              <a href="/auth/logout" class="right">Log out
              </a>
            <% } else { %>
              <div class="google-login">
                <div 
                  id="g_id_onload"
                  data-client_id="<%= googleClientID %>"
                  data-login_uri="/auth/google"
                  data-auto_prompt="false"
                  data-prompt_parent_id="g_id_onload"
                  data-login_hint=""
                ></div>
                <div 
                  id="google-signin-button"
                  class="g_id_signin right"
                  data-type="icon"
                  data-size="medium"
                  data-theme="outline"
                  data-text="sign_in_with"
                  data-shape="pill"
                  data-logo_alignment="left"
                ></div>
              </div>
            <% } %>
          </li>
          <div class="line"></div>
        </ul>
      </div>
    </div>
    <div class="banner">
      <div class="banner-text">
        <div class="banner-text-items">
          <h2 id="powder-peak">powder peak.</h2>
          <p>Experience the natural beauty of pristine slopes, where every run is a brushstroke on winter’s canvas</p>
          <div id="line-home-page"></div>
        </div>
      </div>      
    </div>
  </nav>
